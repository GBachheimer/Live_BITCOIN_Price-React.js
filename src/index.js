import React from 'react';
import ReactDOM from 'react-dom/client';
import HandleData from "./components/HandleData"

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<HandleData />);
